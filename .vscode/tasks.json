{
    "version": "2.0.0",
    "tasks": [
     {
         "label": "buildAndRun",
         "type": "shell",
         "command":"docker build -t remote-debugging-docker . && docker run -d -p 3000:3000 --name debuggable --rm remote-debugging-docker"
     }   
    ]
}